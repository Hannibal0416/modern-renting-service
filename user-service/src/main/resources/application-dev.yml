server:
  port: 8081

oauth2:
  clientId: modern-renting-client
  clientSecret: "modern-renting-secret"
  introspectionUri: http://localhost:9000/oauth2/introspect
  tokenUri: http://localhost:9000/oauth2/token
  revokeUri: http://localhost:9000/oauth2/revoke

spring:
  application:
    name: oauth2

